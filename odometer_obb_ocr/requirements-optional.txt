# Optional OCR engines for odometer_obb_ocr/predict_odometer.py.
# Install only the engine(s) you plan to use via --ocr-engine.
#
# These are optional: importing predict_odometer.py or utils/ocr.py never
# requires either package. Only calling recognize() with a given --ocr-engine
# needs that engine installed.

# PaddleOCR (default --ocr-engine paddle)
paddlepaddle
paddleocr

# EasyOCR (--ocr-engine easy)
easyocr

# Tesseract OCR (--ocr-engine tesseract)
# Requires system installation of Tesseract 5.x binary:
#   Ubuntu/Debian: sudo apt-get install tesseract-ocr
#   CentOS/RHEL:   sudo yum install tesseract
#   macOS:         brew install tesseract
pytesseract
